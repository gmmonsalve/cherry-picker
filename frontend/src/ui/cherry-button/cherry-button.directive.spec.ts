import { CherryButtonDirective } from './cherry-button.directive';

describe('CherryButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new CherryButtonDirective();
    expect(directive).toBeTruthy();
  });
});
